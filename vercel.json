{
  "buildCommand": "pnpm turbo run build --filter=@programionize/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
